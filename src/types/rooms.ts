//Este es el tipo de los probadores, le indicamos que tipos tiene

export interface roomType {
    name: string,
    code: string,
    insideUserCode: string | undefined,
    outsideUserCode: string | undefined
}